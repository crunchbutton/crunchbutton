NGApp.factory('TabsService', function($location) {

	var service = { tabs: {} };

	service.tabs.create = function( params ) {
		var scope = params.scope;
	}

	service.tabs.add = function( params ) {
		var scope = params.scope;
	}

	service.tabs.init = function( params ) {
		var scope = params.scope;
	}

	return service;
});
