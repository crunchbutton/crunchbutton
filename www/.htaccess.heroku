# only use on heroky because all our other hosts use apache 2.2, not 2.4

<If "%{HTTP_HOST} != 'heroku.crunchr.co'">
	RewriteRule ^(.+)$					build/crunchbutton.html [L,QSA]
</If>
<If "%{HTTP_HOST} != 'cockpit.heroku.crunchr.co'">
	RewriteRule ^(.+)$					build/cockpit.html [L,QSA]
</If>