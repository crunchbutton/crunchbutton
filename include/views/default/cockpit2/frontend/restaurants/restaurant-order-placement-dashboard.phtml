<div class="top-pad"></div>

<div class="content-padding" ng-show="ready">

	<form name="form" ng-show="restaurants">

		<ul class="ul-inputs box-content2">
			<li class="li-input">
				<div class="label">Select a restaurant:</div>
				<div class="input">
					<select name="restaurantSelect" ng-model="id_restaurant" ng-change="load_restaurant()" ng-options="opt.id_restaurant as opt.name for opt in restaurants"></select>
				</div>
			</li>
		</ul>
	</form>

	<div class="dashboard-box pending pointer" ng-click="list()">
		<div class="dashboard-count">
			{{status.peding}}
		</div>
		<div class="dashboard-content">
			<div class="title">
				order<span ng-if="status.peding > 1">s</span> pending
			</div>
			<div class="description">
				order<span ng-if="status.peding > 1">s</span> waiting to be picked up
			</div>
		</div>
		<div class="divider"></div>
	</div>

	<div class="dashboard-box transit pointer" ng-click="list()">
		<div class="dashboard-count">
			{{status.transit}}
		</div>
		<div class="dashboard-content">
			<div class="title">
				order<span ng-if="status.transit > 1">s</span> in transit
			</div>
			<div class="description">
				order<span ng-if="status.transit > 1">s</span> waiting to be picked up
			</div>
		</div>
		<div class="divider"></div>
	</div>

	<div class="dashboard-box place-order" ng-click="new()">
		<div>Place a new order</div>
	</div>

	<a href class="dashboard-box call-support" ng-click="callText(status.phone_formatted)">
		<span>Crunchbutton Support</span>
	</a>

	<div class="dashboard-box phone">
		{{status.phone_formatted}}
	</div>

</div>

<spinner-loading></spinner-loading>
<hack-expand-content></hack-expand-content>