<div class="top-pad"></div>

<div class="content-padding" ng-show="ready">


	<h1 class="title left"><i class="fa fa-briefcase"></i><span>Documents</span></h1>

	<div class="divider"></div>

	<p class="link orange" ng-click="pay_info()">Edit Payment information</p><br><br>

	<h2 class="title">
		<span>Documents</span>
	</h2>

	<h3 class="title">
		Here are important docs to fill out and sign. Please download the docs, fill them out and then upload them back to this page. We recommend signing them with <a href="https://www.hellosign.com/" target="_blank">Hellosign</a>.
	</h3>

	<table class="tb-grid">
		<tbody>
			<tr ng-repeat="doc in documents" ng-if="doc.url">
				<td ng-click="edit(driver.id_admin)">
					{{doc.name}}
				</td>
				<td>
					<a href="{{doc.url}}" target="_blank">Download Template</a>
				</td>
				<td>
					<span ng-if="doc.status" class="link orange" ng-click="download( doc.status.id_driver_document_status)" title="Sent at {{doc.status.date_formated}}">Download</span>
					<span ng-if="!doc.status" ng-click="setDocument(doc.id_driver_document)" class="sp-label sp-label-warning link">
						Upload missing doc
					</span>
				</td>
			</tr>
		</tbody>
	</table>

	<h3 class="title">
		Upload a clear picture of your Drivers License and Insurance Card here. You can take a clear, close up picture with a smartphone, and then upload in the appropriate spots below.
	</h3>

	<table class="tb-grid">
		<tbody>
			<tr ng-repeat="doc in documents" ng-if="!doc.url">
				<td ng-click="edit(driver.id_admin)">
					{{doc.name}}
				</td>
				<td>
					<span ng-if="doc.status" class="link orange" ng-click="download( doc.status.id_driver_document_status)" title="Sent at {{doc.status.date_formated}}">Download</span>
					<span ng-if="!doc.status" ng-click="setDocument(doc.id_driver_document)" class="sp-label sp-label-warning link">
						Upload missing doc
					</span>
				</td>
			</tr>
		</tbody>
	</table>

	<form name="docs">

		<h3 class="title">Upload zone</h3>

		<ul class="ul-inputs box-content">
			<li class="li-input">
				<div class="label">Select the file type:</div>
				<select name="driverDocument" ng-model="doc_uploaded" ng-change="setDocument( this.doc_uploaded )" ng-options="opt.id_driver_document as opt.name for opt in documents"></select>
			</li>
			<li ng-show="doc_uploaded">
				<input ng-file-select name="file" type="file" />
			</li>
			<li ng-show="uploader.progress"  class="li-input">
				<span class="sp-label sp-label-info">
					Progress {{uploader.progress}}%
				</span>
			</li>
			<li ng-show="doc_uploaded"  class="li-input">
				<button class="button" ng-click="uploader.uploadAll();">Upload</button>
			</li>
		</ul>

	</form>

</div>

<spinner-loading></spinner-loading>
<hack-expand-content></hack-expand-content>