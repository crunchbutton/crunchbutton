<? if (!$this->suggestions->count()) : ?>
	No results found
<? else : ?>
	<table class="list-suggestions">
		<tr>
			<th>Date</th>
			<th>Name</th>
			<th>Comment</th>
			<th>Restaurant</th>
			<th>Community</th>
			<th>Status</th>
			<th>Type</th>
		</tr>
		<? foreach ($this->suggestions as $suggestion) : ?>
			<? $date = $suggestion->date(); ?>
			<tr class="list-suggestion-item <?=$suggestion->status; ?>">
				<td> <?=$date->format('M jS Y')?><br /><br /><?=$date->format('g:i:s A')?> </td>
				<td> <a href="/admin/suggestions/<?=$suggestion->id_suggestion;?>"><?=$suggestion->name; ?></a> </td>
				<td> <?=$suggestion->content; ?> </td>
				<td> <?=$suggestion->community()->name; ?> </td>
				<td> <?=$suggestion->restaurant()->name; ?> </td>
				<td><span class="status"><?=$suggestion->status; ?></div></td>
				<td> <?=$suggestion->type; ?> </td>
			</tr>
		<? endforeach ; ?>
	</table>
<? endif ; ?>